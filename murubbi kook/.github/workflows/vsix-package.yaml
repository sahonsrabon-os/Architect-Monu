name: VsixPackage

on:
  push:
    branches:
      - main
    tags:
      # Publish to the Marketplace on a semver tag push (e.g. 1.2.0).
      # A tag ref guarantees github.ref == refs/tags/<tag>, which the
      # publish step below relies on. The previous `release: created`
      # trigger could fire during the draft phase with the `main` ref,
      # silently skipping the publish step.
      - '[0-9]+.[0-9]+.[0-9]+'

jobs:
  build:
    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout Extension
        uses: actions/checkout@v4
      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install Extension Dependencies
        # --ignore-scripts blocks dependency lifecycle scripts from running at
        # install time (supply-chain hardening). Safe here: all devDependencies
        # (incl. esbuild, whose platform binary ships as an optionalDependency)
        # work without install scripts.
        run: npm ci --ignore-scripts
      - name: Build Extension
        run: npm run esbuild
      - name: Publish Extension
        if: success() && startsWith(github.ref, 'refs/tags/') && matrix.os == 'ubuntu-latest'
        run: npm run deploy
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
